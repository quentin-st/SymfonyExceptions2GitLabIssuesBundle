{% if title|length > issueTitleMaxLength %}
*{{ title }}*
{% endif %}

`[{{ request.method }}] {{ request.requestUri }}`

{% if user is not empty %}
Logged-in user: {{ user }}
{% endif %}

Exception in *[{{ file ~ '#' ~ line }}]({{ file ~ '#L' ~ line ~ '-' ~ line }})*.

> {{ class }}
> {{ message }}

```
{{ stacktrace|raw }}
```

{% if mentions|length > 0 %}
---
*Mentions:{% for mention in mentions %} @{{ mention }}{% endfor %}*
{% endif %}
